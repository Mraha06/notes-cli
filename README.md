# notes-cli

Консольная утилита для управления текстовыми заметками.

Автор проекта: Могушков Абдул-Рахим
GitHub: https://github.com/Mraha06

---

## Описание проекта

`notes-cli` — консольное Java-приложение, предназначенное для работы с простыми текстовыми заметками. Утилита поддерживает добавление, просмотр, удаление заметок, а также подсчёт их количества.

Все данные сохраняются в файл `data/notes.csv`, что обеспечивает сохранность заметок между запусками приложения, в том числе при работе через Docker.

Проект реализован в рамках учебного задания с использованием:

* **Java 17**
* **Docker**
* **GitHub**
* **GitHub Actions (CI)**
* **SemVer**
* **Pull Request workflow**

---

## Структура проекта

```
notes-cli/
├─ src/
│  └─ com/example/
│     ├─ App.java
│     └─ NotesStore.java
├─ data/
│  └─ notes.csv
├─ Dockerfile
├─ .github/workflows/ci.yml
├─ .gitignore
└─ README.md
```

---

## Поддерживаемые команды

Управление утилитой осуществляется через аргументы командной строки.

### Добавление заметки

```
--cmd=add --text="Купить хлеб"
```

Результат:

```
Added #1
```

---

### Просмотр всех заметок

```
--cmd=list
```

Пример вывода:

```
1;Купить молоко
2;Позвонить другу
```

Если файл пуст:

```
(empty)
```

---

### Удаление заметки по ID

```
--cmd=rm --id=1
```

Если запись не найдена:

```
Not found #4
```

---

### Подсчёт количества заметок

```
--cmd=count
```

Вывод:

```
3
```

---

## Локальный запуск (без Maven)

Перейдите в корень проекта.

### Компиляция

```
javac src/com/example/*.java
```

### Запуск

```
java -cp src com.example.App --cmd=add --text="Test"
java -cp src com.example.App --cmd=list
java -cp src com.example.App --cmd=rm --id=1
java -cp src com.example.App --cmd=count
```

---

## Docker

### Сборка Docker-образа

```
docker build -t notes-cli:dev .
```

### Запуск контейнера

Создание каталога для хранения данных:

```
mkdir -p data
```

Добавление заметки:

```
docker run --rm -v "$PWD/data:/app/data" notes-cli:dev --cmd=add --text="Docker note"
```

Просмотр списка:

```
docker run --rm -v "$PWD/data:/app/data" notes-cli:dev --cmd=list
```

Удаление заметки:

```
docker run --rm -v "$PWD/data:/app/data" notes-cli:dev --cmd=rm --id=1
```

Подсчёт заметок:

```
docker run --rm -v "$PWD/data:/app/data" notes-cli:dev --cmd=count
```

Все заметки сохраняются в `data/notes.csv` на хост-машине и не пропадают после остановки контейнера.

---

## Версионирование (SemVer)

### v1.0.0

* Реализованы команды:

  * `add`
  * `list`
* Первый стабильный релиз

### v1.1.0

* Добавлены команды:

  * `rm` — удаление заметки по ID
  * `count` — подсчёт количества заметок
* Изменения внесены через feature-ветку и Pull Request

---

## CI (Continuous Integration)

В проекте используется GitHub Actions для автоматической проверки сборки:

* запуск при `push` в любую ветку
* запуск при `pull_request` в `main`
* установка Java 17
* компиляция `.java` файлов через `javac`

Workflow расположен по пути:

```
.github/workflows/ci.yml
```

---

## Примеры заметок

```
"Купить молоко"
"Позвонить другу"
"Сделать домашку"
```

Хранятся в файле:

```
data/notes.csv
```

---

## Итог

Проект демонстрирует разработку консольного Java-приложения с:

* файловым хранилищем
* Docker-окружением
* CI-проверкой
* Git-flow с Pull Request
* семантическим версионированием

---

🚀 Готово к запуску локально и в Docker
